<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dolinsek.elias.cashcockpit">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:windowSoftInputMode="adjustPan|stateAlwaysHidden" />
        <activity android:name=".BankAccountActivity" android:theme="@style/AppTheme.ActionBarEnabled" />
        <activity android:name=".CategoryActivity" android:theme="@style/AppTheme.ActionBarEnabled" />
        <activity
            android:name=".AutoPayActivity"
            android:windowSoftInputMode="adjustResize|stateAlwaysHidden" android:theme="@style/AppTheme.ActionBarEnabled"/>
        <activity
            android:name=".BillEditorActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".SelectCategoryActivity" android:theme="@style/AppTheme.ActionBarEnabled" android:label="@string/label_select_category"/>
        <activity
            android:name=".StartActivity"
            android:noHistory="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PasswordPreferenceActivity" android:theme="@style/AppTheme.ActionBarEnabled" android:label="@string/label_password_settings"/>
        <activity android:name=".CockpitChartPreferencesActivity"
            android:windowSoftInputMode="stateAlwaysVisible" android:theme="@style/AppTheme.ActionBarEnabled" android:label="@string/label_cockpit_chart_preferences"/>
    </application>

</manifest>